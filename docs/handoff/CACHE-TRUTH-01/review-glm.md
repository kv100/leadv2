Все проверки завершены. Сводка проверки (артефакты представлены ниже):

- Набор тестов запущен в рабочем дереве: `PASS=10 FAIL=0`, RC=0 — подтверждает утверждение в отчете.
- Проверка dispatch-c293c1d5: 176 событий использования ассистента, но **95 уникальных идентификаторов сообщений; 76 дублирующихся идентификаторов, все 76 с идентичным использованием; уникальная сумма cache_read = 15 070 604 против суммы событий 27 236 567**.
- Проверка freepool 260901-175619-repo-53e9: **136 из 137 сообщений ассистента содержат ТОЛЬКО ключи использования `('input_tokens','output_tokens')`; ровно 1 сообщение содержит ключи кэша (все значения 0)**.
- glm run: 215 сообщений, все `(0, 0)`, альтернативное расположение использования отсутствует — утверждение glm в отчете подтверждено.
- kimi run: 0 сообщений ассистента — подтверждено.
- Ссылки на код: `glm-coder.sh:1830` — это `export DISABLE_MODEL_AVAILABILITY_CHECK=1` (реальная сборка подсказки находится на строках 346/1891); `glm-coder.sh:363` — это `--max-turns)` (реальный `--strict-mcp-config` на 371/1192); `kimi-coder.sh:178` — комментарий к `refusal_reason`; `leadv2-dispatch-code.sh:2997` — несвязанный python-код для вердикта.
- Сопоставление `tests/run-all.sh` и логика поиска `stem` (`key == stem || key == stem.sh`) — строки настроены корректно.

РЕЗУЛЬТАТ РЕЦЕНЗИИ: FAIL
РЕЗУЛЬТАТ РЕЦЕНЗИИ: critical=0 high=2 medium=3 low=3
НАЙДЕНО: severity=High file=plugins/leadv2/scripts/leadv2-cache-truth.sh line=119 dimension=correctness desc=Нет дедупликации по message.id: дублирующиеся события ассистента с идентичным использованием завышают количество запросов и итоговые значения токенов в 1.81 раза (176 событий против 95 уникальных; 27.24M cache_read против 15.07M в dispatch-c293c1d5) — ключевые абсолютные значения инструмента и таблица отчета неверны.
НАЙДЕНО: severity=High file=docs/handoff/CACHE-TRUTH-01/report.md line=60 dimension=correctness desc=Утверждение о freepool «ключи ПРИСУТСТВУЮТ, но всегда равны 0 в течение 137 запросов, проверено путем прямой инспекции JSON» ложно: только 1/137 запросов содержит ключи кэша (значения 0), 136 не сообщают их; `saw_cache_key` в инструменте является глобальным для запуска, поэтому смешанные потоки ошибочно классифицируются как «реальные 0.0000», что противоречит заявленной философии самого инструмента (отсутствие против нуля).

## Рецензия — docs/handoff/CACHE-TRUTH-01/build-attempt-1.diff

### Критично
Нет.

### Высокий

**H1 — Отсутствие дедупликации message.id завышает все абсолютные значения (корректность).**
`leadv2-cache-truth.sh:119-127` суммирует использование для каждого *события* stream-json. В реальных потоках отправки одно сообщение ассистента генерирует несколько событий использования (по одному на каждый блок контента), все с идентичным использованием на уровне сообщения. Проверка на dispatch-c293c1d5:
```
assistant_usage_events 176 unique_msg_ids 95
dup ids: 76 with identical usage: 76
unique-sum cache_read: 15070604 vs event-sum 27236567
```
Таким образом, строка таблицы отчета `turns=176 / cache_read=27,236,567` и вывод «27.2M токенов cache_read против 352 свежих входных токенов» (report.md:43-46) завышены примерно в 1.81 раза; реальный запуск составил 95 запросов / ~15.07M cache_read. `hit_ratio=0.9802` не изменяется (дубликаты пропорциональны), а `first_break` индексируется по событию, а не по запросу, поэтому номер запроса также неточен. Асинхронное обнаружение в том же Lane (open-threads 2026-09-01T22:38Z) пометило именно это («суммируйте после дедупликации по message.id»); инструмент игнорирует это.

**H2 — Основное утверждение о freepool фактически неверно; смешанные потоки ошибочно классифицируются как «сообщенный ноль» (корректность + утверждения-без-доказательств).**
В отчете говорится (report.md:60-62), что для запуска `260901-175619-repo-53e9` «ключи cache_read/cache_creation ПРИСУТСТВУТ, но всегда равны 0 в течение 137 запросов (реальное соотношение 0.0000, проверено путем прямой инспекции JSON, а не пробел в парсинге)». Живая проверка опровергает это:
```
== freepool-runs/260901-175619-repo-53e9
assistant 137
136 ('input_tokens', 'output_tokens')          <- NO cache keys
1   ('cache_creation','cache_creation_input_tokens','cache_read_input_tokens', ...)  values all 0
```
Один-единственный запрос с ключами кэша (все нулевые) переключает `saw_cache_key` (script line 120-122), и 136 запросов с *отсутствующими* ключами незаметно преобразуются в сообщенные нули — именно то принуждение, которое заявлено как запретленное в заголовке самого инструмента («отсутствующее поле и сообщенный ноль — это разные факты»). Таблица показывает `0.0000` для этого запуска и `unreported` для запуска-потомка; реальный вывод — «смешанный: 1/137 сообщенных, все нули». Направление вывода (на стороне провайдера) выживает, но доказательства, представленные как проверенные путем прямой инспекции, не подтверждаются, а Фикстура 4 обоснована этим конкретным примером с реальными данными, которого не существует.

### Средний

**M1 — Неверные ссылки на строки кода (перечень, 4 экземпляра одного и того же типа).** Каждая проверенная ссылка на строки в report.md указывает на несвязанный код:
- report.md:43 `leadv2-dispatch-code.sh line ~2997` → фактически python-код вердикта заданий;
- report.md:86-88 `glm-coder.sh:1830` → фактически `export DISABLE_MODEL_AVAILABILITY_CHECK=1` (реальная сборка подсказки: glm-coder.sh:346 и :1891);
- report.md:96-97 `glm-coder.sh:363` для `--mcp-config`/`--strict-mcp-config` → фактически аргумент case `--max-turns)` (реальный: glm-coder.sh:371, :1192);
- report.md:77 `kimi-coder.sh:178` для пути повторной маршрутизации при сбое пробы → фактически комментарий к `refusal_reason()`.
Только `claude-subsession.sh:536` выглядит верно. Основные утверждения (промпт = PREAMBLE+mission+TRAILER, `--max-turns` на всех ветвях — оба перепроверены) подтверждаются, но ссылки представляют собой непроверенные доказательства, представленные как проверенные.

**M2 — Строки `EXTRA_SUITE_MAP` для runner→test-cache-truth связывают несвязанные наборы тестов (дизайн).** tests/run-all.sh:229-232 сопоставляет `glm-coder.sh`, `freepool-coder.sh`, `kimi-coder.sh`, `claude-subsession.sh` с `test-cache-truth.sh`, набором тестов, который вообще не запускает эти скрипты. Каждый существующий элемент карты семантически означает «измененный файл → набор, который его охватывает» (например, `glm-coder.sh → test-glm-lock-per-lane.sh`); они означают «измененный runner → запуск несвязанного набора тестов», добавляя шум для будущих изменений runner без покрытия. Строка `leadv2-cache-truth.sh` — это правильный и достаточный элемент.

**M3 — Реальный случай со смешанными ключами не протестирован (тесты-могут-завершиться-неудачей).** Фикстуры 3 и 4 охватывают чистое отсутствие и чистое присутствие ключей кэша; смешанный поток (некоторые запросы сообщают ключи, некоторые нет) — это форма, которая действительно существует в реальных данных freepool, и это именно та форма, в которой баг H2 выживает при каждом чистом фикстуре. Никакая мутация `saw_cache_key` / веток классификации не контролируется — мутация затрагивает только формулу соотношения.

### Низкий

- **L1** — `leadv2-cache-truth.sh:127-129`: поток с нулевым запросом выводит `first_break=none`, но docstring (строки 24-26) говорит, что `unreported` заменяет оба столбца, когда ключи кэша отсутствуют; «никогда не опускался ниже 0.5» не имеет смысла для потока без запросов (строка kimi в таблице скрывает это).
- **L2** — report.md:76: «73 строки `"type":"system"` только» для прогона kimi — я подтвердил 0 сообщений ассистента, но не количество строк system; на решение не влияет, оставлено непроверенным в отчете.
- **L3** — report.md:135-140: «Проверено симуляцией поиска по stem» для связывания run-all не содержит вставленного артефакта (я проверил это после факта: строки присутствуют в tests/run-all.sh:228-232, а логика соответствия `key == stem || key == stem.sh` в tests/run-all.sh:390-394 разрешает все пять); честный отказ от запуска `--scope changed` — это хорошо.

### Что подтверждено как верное
glm «всегда `(0,0)` при 215 сообщениях, без альтернативного расположения использования» — проверено (census `{(0, 0): 215}`, ключи верхнего уровня `['message','parent_tool_use_id','session_id','timestamp','type','uuid']`). kimi 0 сообщений ассистента — проверено. Набор тестов зеленый, 10 PASS, <2 сек — проверено. Встроенный элемент управления мутацией действительно красный (0.3691 против 0.62) и самопроверяется от дрейфа sed. Арифметика фикстуры, семантика прерывания и семантика rc/ошибки проверены и верны.

---
ЗАВЕРШЕНИЕ КОНТРАКТА: измененные файлы — нет (только рецензия; все проверки были доступны только для чтения или /tmp). Тестовые результаты: проверенный набор test-cache-truth.sh PASS=10 FAIL=0, RC=0; вышеуказанные проверки на Python подтверждены. НЕ ЗАФИКСИРОВАНО — рецензия не внесла изменений в рабочее дерево; нечего фиксировать. Никакие stash-хранилища не создавались.
