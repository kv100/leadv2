# CODEX-FULL-LEAD-FROM-TERMINAL-01 — `/leadv2 codex [task-id|next]`

Repo: ~/Projects/leadv2 (canonical plugin). Founder order 2026-08-03: sometimes Codex should be the FULL lead for one task, invoked from the founder's own terminal via /leadv2 — no separate app. Design accepted by founder:

- New invocation `/leadv2 codex [task-id|next]`. The Claude session does Phase-0 INTAKE ONLY: resolve the task (explicit id, or `next` = top open row of docs/tasks.yaml by priority that is queued+unclaimed+unblocked), run the dup-guard (no ledger open row / live worker / claim on the same sig), claim the task, create the lane worktree, then hand the WHOLE 0..8 cycle to `plugins/leadv2/scripts/leadv2-codex-session-runner.sh` (already exists, proven in fanout — reuse, do not rewrite).
- After handoff Claude is a THIN RELAY: it arms a watcher on the runner's phase/question/final sentinels and speaks only at Gate-1, on an async question, and at Phase-8 close (validate receipt + commit ancestry; the live ladder stays the lead's manual step). No polling, no narration.
- Cost note (founder concern): the invocation MUST print an advisory line at start: this session needs no Opus brain — recommend launching such sessions with `/model sonnet`; the thinking runs on Codex.

IMPLEMENT:
1. `plugins/leadv2/scripts/leadv2-codex-lead.sh` — the intake wrapper. Args: `[task-id|next]` (default next). Steps: resolve task from docs/tasks.yaml (read-only helpers from leadv2-tasks-lib.sh where possible), dup-guard against dispatch ledger + job registry + live claim, claim/lease via tasks-lib, create worktree `.claude/worktrees/<sig8>`, then exec the session-runner with the task/worktree (read the runner's actual argument contract from its source first and match it exactly). Print: chosen task, worktree, runner handle/log path, the /model sonnet advisory, and the three relay wake-points. Non-interactive; exit non-zero with a one-line reason on any guard refusal (no partial state left behind — unclaim on failure).
2. `plugins/leadv2/commands/leadv2.md` — add the `/leadv2 codex [task|next]` row to the invocation table with the relay contract (wake only at Gate-1/question/Phase-8; validated Phase-8 sentinel = completion; lead personally climbs the live ladder after). ALSO fix stale planner pins in this doc: lines ~89 and ~110 still say "architect sonnet ALWAYS / critic sonnet by default" — update to PLANNER-MODELS-DECISION-01 (architect/synth = opus Standard / fable Heavy, efforts high/xhigh; Codex same-tier second brain standard↔opus, top↔fable; GLM/Kimi build-only, never plan roles) to match plugins/leadv2/workflows/leadv2-plan.js (commit 2ae3016).
3. Tests: a stubbed test suite `plugins/leadv2/scripts/tests/test-codex-lead-intake.sh` — stub tasks.yaml + ledger + runner binary (record-args stub): (a) `next` picks the top eligible row and passes the exact expected args to the runner stub; (b) explicit task-id resolves; (c) dup-guard refusal when an open claim/live handle exists — non-zero exit, task left unclaimed; (d) guard-failure path leaves no worktree/claim residue; (e) advisory line present in output. Register in run-core-offline.sh.

ACCEPTANCE: bash -n all touched; new suite green; run-core-offline.sh green vs current-main baseline (22/22). Report: runner arg contract you matched, per-item status, test summary. Do NOT commit.
NON-GOALS: no changes to leadv2-codex-session-runner.sh itself, no supervisor/fanout paths, no routing yaml.
Rollback: git checkout of touched files.
