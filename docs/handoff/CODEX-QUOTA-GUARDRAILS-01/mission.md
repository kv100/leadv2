# CODEX-QUOTA-GUARDRAILS-01 — codex spend guardrails: effort pinned, no ungated spawns, circuit-open on usage-limit

Repo: ~/Projects/leadv2 (canonical plugin). Founder order 2026-08-03 ("внедри фиксы по квоте кодекса"), from RCA CODEX-QUOTA-BURN-RCA-01: the weekly codex window (Aug 1 08:49 → Aug 8 08:49) burned out in ~2 days. Ranked multipliers: (1) direct `codex exec` runs default to reasoning effort XHIGH and bypass the router's quota gate entirely; (2) failure double-billing — dead workers burn full repo-context input, the retry burns it again; (3) 25M-token cached-input sessions. 68 sessions in-window.

REQUIREMENTS:
1. EFFORT PINNED TO STANDARD ON EVERY SPAWN PATH: audit every place the plugin invokes the codex CLI or codex-companion and ensure the reasoning effort is EXPLICITLY pinned (never left to the CLI default, which is xhigh for `codex exec`):
   - leadv2-codex-session-runner.sh:455,473 — already threads $EFFORT (default medium via LEADV2_LEAD_EFFORT); verify both invocations always receive it and no other cmd= construction lacks it.
   - codex-task.sh --tier resolution — tier table maps to model+effort; default tier must resolve to standard effort (feedback-codex-tier-default-is-standard). Verify the NO-tier path also pins effort explicitly.
   - leadv2-codex-planner.sh, dispatch-code codex arm, leadv2-codex-lead.sh relay, any review invocations — same: explicit effort, default standard/medium; xhigh permitted ONLY when the caller passes an explicit top-tier review flag.
   List every invocation site found and its before/after in the report.
2. QUOTA GATE BEFORE EVERY SPAWN: every codex spawn path must consult the existing quota gate (the same lib/leadv2-glm-policy-resolve.py codex_quota_gate the router uses) BEFORE launching. A path that cannot reach the resolver fails toward NOT spawning codex (spill to the ladder), never toward an ungated spawn.
3. CIRCUIT-OPEN ON USAGE-LIMIT: when a codex invocation's output matches the usage-limit refusal (the literal shape: "You've hit your usage limit" / "try again at <date>"), write a circuit marker (state file with the retry-at timestamp parsed from the message when present) that the quota gate reads: while the circuit is open, the codex arm is excluded and the ladder spills (glm→sonnet per policy). Circuit auto-closes when now >= retry-at (or after 24h if unparseable). Marker location: the shared control-plane state dir (leadv2-state-path.sh), so all sessions/worktrees of the repo see one circuit. Journal `codex_circuit_open until=<ts>` once at open, not per refusal.
4. LEAD DIRECT-EXEC GUARD: a PreToolUse-style hook (pattern: existing hooks in plugins/leadv2/hooks/) that intercepts Bash commands matching a direct `codex exec` invocation from a LEAD session (not from the plugin's own runner scripts — those set an env marker, e.g. LEADV2_CODEX_SANCTIONED=1, that the hook honors) and denies with a one-line pointer to codex-task.sh/the router. Escape hatch: LEADV2_ALLOW_DIRECT_CODEX=1 env (logged). Hook must be additive in hooks.json and fail-open on its own errors (never block unrelated Bash).
5. NON-GOALS: no changes to the review-arm selection policy, no new quota accounting (reuse the existing gate/ledgers), no changes to GLM/kimi paths, no touching leadv2-dispatch-product-close.sh or leadv2-dispatch-code.sh spawn logic beyond the codex-arm gate consult (and if another lane currently holds either file, coordinate scope to the codex-specific functions only).
6. TESTS: new suite with stubbed codex binary + stubbed state dir: (a) each spawn path passes explicit effort (stub records argv; assert model_reasoning_effort present and standard by default); (b) xhigh only with explicit top flag; (c) usage-limit output opens the circuit with parsed retry-at; gate excludes codex while open; auto-close at expiry; (d) hook denies bare `codex exec`, allows when LEADV2_CODEX_SANCTIONED=1 or LEADV2_ALLOW_DIRECT_CODEX=1, never blocks non-codex Bash; (e) resolver-unreachable → no codex spawn (spill), not an ungated spawn. Register in run-core-offline.sh.

ACCEPTANCE: bash -n (bash5 + /bin/bash 3.2); focused suite green; run-core-offline.sh green vs current-main baseline (verify count first — it grew several times today). Report per-requirement status incl. the invocation-site audit table. Do NOT commit.
Rollback: git checkout of touched files + rm of the new hook file.
