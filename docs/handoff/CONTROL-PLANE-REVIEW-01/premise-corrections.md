# Premise corrections — read before trusting f1..f4

## M2 (arbiter), spine item 2 — CONTAMINATED

The lead handed M2 a false premise: that the arbiter "silently drops"
`"min_capability": 4`, evidenced by an identical reply with and without the key.
That inference has no discriminating power — `codex-terra` IS capability 4, so the
default pick already satisfied the floor, and an identical reply is the CORRECT
reply. The probe could not tell "honoured" from "ignored".

A SendMessage correction was sent to the agent, but it did not reach the work:
the codex-rescue agent had already completed (it is a launcher, not a worker) and
its background codex task `task-mtwbhutf-957bng` carries the original prompt.
After launch the prompt is unfixable — filed as
`CODEX-AGENT-COMPLETION-MEANS-QUEUED-NOT-DONE-01`.

So when f2-arbiter.md lands: **discount its item 2 unless it cites the JSON parse
site by file:line.** The discriminating test is a floor the default pick does NOT
meet (one only `codex-sol` satisfies, or one nothing satisfies — a refusal proves
the key is read). The lead owes that verification himself.

Everything else in M2, and all of M1/M3/M4, was briefed from measured facts.
