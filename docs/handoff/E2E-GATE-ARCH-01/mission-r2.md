E2E-GATE-ARCH-01 fix round r2 (last round). Worktree: /Users/kostiantyn.vlasenko/Projects/leadv2/.claude/worktrees/E2E-GATE-ARCH-01, on top of 5c7cc5c. Review findings (verbatim, all in plugins/leadv2/scripts/leadv2-dispatch-product-close.sh gate block ~:1605-1625 unless noted):

1. CRITICAL :1622 — "Lock-file redirect failure makes the whole e2e step return rc=1 with no e2e-gate.log, so an infra fault is stamped as e2e_regression and the lane exits 8 (false dead) — reproduced." Infra faults (lock dir/redirect failure) must be classified distinctly (e.g. e2e_infra) and must NOT kill the lane as e2e_regression; degrade to running the gate without the lock, with an explicit journal line.
2. HIGH :1611 — "Where flock is absent (stock macOS) both attempts fail 127 and the gate runs unlocked — the fix silently no-ops, log lies 'lock contention'." Detect `command -v flock` up front; if absent, use a portable fallback (mkdir spin-lock with stale-pid cleanup) instead of lying logs.
3. HIGH :1610/:1612 — "After two lock timeouts the gate proceeds UNLOCKED, defeating one-gate-at-a-time under exactly the contention being fixed, and the identical retry doubles dead time (2×300s)." Replace retry-then-unlocked with: single bounded wait (LEADV2_E2E_GATE_LOCK_TIMEOUT), on timeout DO NOT run unlocked — requeue/park the gate attempt with a distinct journal line (e2e_gate_lock_timeout) and retry the gate once after a short jitter; if still held, fail the STEP as infra (not regression), lane stays alive.
4. HIGH test-e2e-gate-arch-01.sh — "retry, unlocked fallback, lock-dir creation branches have zero coverage." Cover every new branch: no-flock fallback path, lock timeout path (short timeout env), infra-fault classification. All mutation-gated (each test FAILS with its branch reverted — run and report rcs).

Keep: gate runs suites against the LANE worktree (existing behavior from 5c7cc5c), timing kill-switches untouched.
Run bash plugins/leadv2/scripts/tests/test-e2e-gate-arch-01.sh green. ONE commit: "fix(gate): E2E-GATE-ARCH-01 r2 — infra-fault classification, portable lock (no-flock fallback), no unlocked fallback on timeout — mutation-gated". Do NOT push/merge. Final line: sha + rcs + DELIVERABLE_COMPLETE.
