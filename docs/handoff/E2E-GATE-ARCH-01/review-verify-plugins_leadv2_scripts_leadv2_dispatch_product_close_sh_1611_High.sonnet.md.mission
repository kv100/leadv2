Try to REFUTE this finding. Default is_real=false if you cannot concretely confirm it against docs/handoff/E2E-GATE-ARCH-01/build-attempt-1.diff.
Finding [High/correctness]: Where flock is absent (stock macOS, no util-linux) both attempts fail 127 and the gate runs unlocked — the fix silently no-ops, lane passes with bypassed:false and log says "lock contention" — reproduced.
Report exactly one line: VERIFY_VERDICT: <upheld|refuted>
