Try to REFUTE this finding. Default is_real=false if you cannot concretely confirm it against docs/handoff/E2E-GATE-ARCH-01/build-attempt-1.diff.
Finding [High/design]: Identical retry doubles worst-case wait to 2x300s and then runs UNLOCKED concurrently with the holder, so under the exact many-lane load being fixed the lock degrades to nothing while adding 10min dead time per lane.
Report exactly one line: VERIFY_VERDICT: <upheld|refuted>
