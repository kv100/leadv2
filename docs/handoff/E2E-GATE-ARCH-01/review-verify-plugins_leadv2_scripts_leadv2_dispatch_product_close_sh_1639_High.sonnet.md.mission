Try to REFUTE this finding. Default is_real=false if you cannot concretely confirm it against docs/handoff/E2E-GATE-ARCH-01/build-attempt-2.diff.
Finding [High/correctness]: mkdir stale-lock reclaim is a TOCTOU — a waiter can rm/rmdir a lock dir that a third process legitimately re-acquired between the pid read and the rmdir, granting the lock to two lanes at once
Report exactly one line: VERIFY_VERDICT: <upheld|refuted>
