Try to REFUTE this finding. Default is_real=false if you cannot concretely confirm it against docs/handoff/E2E-GATE-ARCH-01/build-attempt-1.diff.
Finding [High/correctness]: Three new branches (retry, unlocked fallback, lock-dir creation) have zero test coverage; only the happy locked path is exercised.
Report exactly one line: VERIFY_VERDICT: <upheld|refuted>
