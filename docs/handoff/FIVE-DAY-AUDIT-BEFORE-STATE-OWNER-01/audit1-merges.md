## Audit results — leadv2 plugin, merges since 2026-08-29

Method note: `git log --merges` returns 84 commits, but ~45 are pure sync merges (`Merge branch 'main' into worktree-X`) with no independent claim — I traced those to their actual landing point (regular commits on the worktree branch, confirmed `git merge-base --is-ancestor &lt;tip&gt; HEAD`) rather than auditing them separately. Grouped by feature below.

| feature | merge(s) | claim | verdict | proof command |
|---|---|---|---|---|
| Anti-fork guard for hooks | 0e169fbc PULSE-HOOK-IS-A-FORKED-COPY-01 | "anti-fork guard for plugin hooks in consumer repos" | **DEAD** | `grep -rn "hook-fork-guard" plugins/leadv2/hooks/*.json plugins/leadv2/scripts/*.sh` → zero hits outside `leadv2-hook-fork-guard.sh` itself and its own test |
| Fork-budget sweep | 1d179855 FORK-STORM-KILLS-HOOKS-01 | "fork-free waits, orphan reaping" via `leadv2-hook-fork-budget.sh` | **DEAD** | `grep -rn "hook-fork-budget" plugins/leadv2` → only self-references and `test-hook-fork-budget.sh`; not in `hooks.json` |
| CI runs the suites | fec881ae CI-RUNS-THE-SUITES-01 | "CI runs the suites, known-red allow-list reaches the lane-facing decision" | **DEAD** (as CI) | `git rev-list --left-right --count origin/main...HEAD` → HEAD is 26 commits ahead, unpushed; `git show origin/main:.github/workflows/test-suites.yml` → fatal, not on origin; `gh run list --workflow=test-suites.yml` → `HTTP 404: workflow not found on the default branch`. The known-red plumbing inside `tests/run-all.sh` is real and local, but GitHub has never executed this workflow once |
| Lane watch v2 (WATCHER-LEAK-01, ONE-LANE-WATCH-01) | c44de765, c49cc9fb | watcher outlives session; single self-arming watcher | LIVE | `grep -n lane-watch-v2 plugins/leadv2/hooks/hooks.json` → SessionStart/PreToolUse arm+disarm commands present |
| Guard census + verdict recording (GUARD-CENSUS-IS-WRONG-01) | 028da826 + non-merge commits (ancestor-confirmed) | census derives from behaviour, not self-description | LIVE | `sed -n '110,135p' plugins/leadv2/hooks/leadv2-bash-pre-dispatch.sh` shows the runner recording every guard's exit into `leadv2-guard-verdict.sh`; `grep -n bash-pre-dispatch plugins/leadv2/hooks/hooks.json` confirms the dispatcher itself is wired |
| Nudge threshold hooks | acece4cb NUDGE-TAX-01 | loud hooks fire on threshold crossings only | LIVE | edits `leadv2-lead-delegation-nudge.sh` + `leadv2-loop-detect.py` in place; both already wired in `hooks.json` (`leadv2-loop-detect-hook.sh` appears twice) |
| Phase-bootstrap deadlock fix | d2ae0172 | first Standard/Heavy dispatch can pass the phase gate | LIVE | direct edit to `leadv2-dispatch-code.sh`, the live dispatcher entrypoint |
| Route arbiter / complexity / effort / arms-admission chain (COMPLEXITY-ESTIMATOR-IS-OFF-01, EFFORT-IS-NOT-WIRED-01, ARMS-ADMISSION-01, GLM-ARM-THROUGHPUT-01) | dc68d743, 720fba14, ba813e8a, beb11434 | routing signals reach the arbiter | LIVE | all edit `leadv2-dispatch-code.sh`, `lib/leadv2-route-arbiter.sh`, `leadv2-routing.yaml` directly — the live routing path |
| Lead↔worker channel (inbox/notify) | 20fca344 LEAD-WORKER-CHANNEL-01 | worker→lead notifier + lead inbox | LIVE | `grep -n leadv2-inbox plugins/leadv2/scripts/leadv2-broad-status.sh` → `INBOX_MD=$(... leadv2-inbox.sh drain --lead ...)`, and `ask-lead.sh`/`leadv2-dispatch-code.sh` also call it |
| Suite-that-cannot-fail / suite-falsifiable gate | 7b9faacb | "a suite must prove it can go red, review gate refuses one that cannot" | LIVE | `grep -n suite-falsifiable plugins/leadv2/scripts/leadv2-review-run.sh tests/run-all.sh` → both call it |
| Repo-install / adoption gate | 83f3f349 ADOPTION-GUARANTEES-A-PASSABLE-GATE-01 | every repo gets a committable phase gate | LIVE | `leadv2-repo-install.sh` called from `leadv2-command-bootstrap.sh`, which is wired in `hooks.json` |
| Watcher-lifecycle singleton (WATCHER-LIFECYCLE-LEAK-01) | d61b2246 | watcher singleton + TERM self-reap | LIVE | `lib/leadv2-watch-lifecycle.sh` called from `leadv2-single-lead-beat-loop.sh`, `leadv2-dispatch-ledger.sh`, `leadv2-lane-pulse-watch.sh` |
| Cache-truth / status-churn / brain-class-live / workers-must-commit (worktree branches, no discrete "landing" merge) | many non-merge commits | assorted caching, status-collector, commit-hygiene fixes | LIVE (merged) | `git merge-base --is-ancestor &lt;branch-tip&gt; HEAD` → YES for all four; changes touch already-wired consumers (`leadv2-broad-status.sh`, `leadv2-dispatch-code.sh`) |
| Review-verdict counter, gate-proves-own-control, review-run-loses-verdicts, gate-origin-main, dispatch-close-gate, handoff-artifacts-gitignored, hook-output-cap, freepool, promise-guard-bind, pulse-repo-scoped, plugin-papercuts, codex-detach, codex-dies-mid-test, close-gate-a2 | 4a9c1637, de44cc73, 7927b0f2, 950b5943, 6b5d6515/baae58eb, 5f5de838, 42d32322, 0b0bb0b0, 1c5d89f9, 8248df10, c01e9838, 775d2df4, 9813ca04, 7c59b87a | assorted hardening of the review/dispatch/pulse path | LIVE | all edit already-wired live files directly (`leadv2-review-run.sh`, `leadv2-broad-status.sh`, `leadv2-dispatch-code.sh`, `.gitignore`, `codex-task.sh`); no new orphaned entrypoints found |

**Summary:** Of ~35 distinct features audited, 32 are LIVE (directly reachable — mostly because they patch already-wired core files like `leadv2-dispatch-code.sh`, `leadv2-broad-status.sh`, `leadv2-review-run.sh`, or `hooks.json` itself, so wiring came for free), and 3 are DEAD: `leadv2-hook-fork-guard.sh` and `leadv2-hook-fork-budget.sh` (both shipped as standalone scripts with zero production callers — only their own tests reference them), and the entire CI-RUNS-THE-SUITES-01 claim (the `.github/workflows/test-suites.yml` file exists on disk but was never pushed — origin/main is 26 commits behind local HEAD and `gh run list` returns a 404, so GitHub has never run it once despite the commit message declaring "CI runs the suites"). No UNPROVEN verdicts were needed — static wiring evidence was decisive in every case.

DELIVERABLE_COMPLETE