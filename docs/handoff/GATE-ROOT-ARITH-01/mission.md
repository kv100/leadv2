# GATE-ROOT-ARITH-01 — run-core-offline REPO_ROOT must be derived from git, not ../.. hops

Repo: ~/Projects/leadv2 (canonical plugin). Live-confirmed 2026-08-04: persona-engine's `.claude/scripts/tests/run-core-offline.sh` is a SYMLINK to canonical `plugins/leadv2/scripts/tests/run-core-offline.sh`, whose header (lines 8-10) computes `REPO_ROOT` via two `../..` hops assuming the file sits 4 segments deep. Through the 3-deep symlink path, `dirname BASH_SOURCE` yields the SYMLINK's logical dir → `PLUGIN_ROOT=persona-engine/.claude`, `REPO_ROOT=/Users/kostiantyn.vlasenko/Projects` (the parent of ALL checkouts). This is the same wrong-root arithmetic family that produced 5 false dead lanes in leadv2 on Aug 3 (that repo's repo-root COPY of the same file, 3 deep, same overshoot — see docs/handoff/dispatch-3c663543/e2e-gate.log line `repo=/Users/kostiantyn.vlasenko/Projects`).

REQUIREMENTS:
1. In canonical plugins/leadv2/scripts/tests/run-core-offline.sh replace the `../..` REPO_ROOT derivation with `REPO_ROOT="$(git -C "$TEST_DIR" rev-parse --show-toplevel 2>/dev/null)"` — TEST_DIR stays LOGICAL (unresolved symlink side) so a persona-engine symlink invocation resolves to persona-engine's root, and a canonical invocation (incl. from any leadv2 worktree) resolves to that worktree's root. PLUGIN_ROOT: derive relative to the PHYSICAL script location (resolve BASH_SOURCE via `cd -P`/readlink chain, bash-3.2-safe — no `readlink -f` dependency assumptions without a fallback) since plugin-internal relative paths (TEST_DIR siblings) must keep pointing at the canonical plugin tree regardless of symlink entry.
2. HARD GUARD: if REPO_ROOT is empty (not a git checkout) or equals a directory with no `.git` marker, print a one-line named error (`repo_root_unresolvable`) and exit nonzero BEFORE running any suite — never silently fall back to a parent directory.
3. Audit the same header pattern in the repo-root COPY at leadv2's `.claude/scripts/tests/run-core-offline.sh` and in the leadv2 `tests/run-all.sh` search-path logic (GATE-WRONG-ROOT just made the copy unreachable for leadv2's own gate — do NOT undo that; but if the copy remains reachable via other repos' symlinks, apply the same git-derivation fix there or replace the copy with a symlink to canonical, whichever is smaller). List every file carrying the `../..` root pattern in the report.
4. NON-GOALS: no changes to which suites run (registration list untouched), no changes to other repos' files (persona-engine/m3 symlinks already point at canonical — fixing canonical fixes them), no gate-logic changes in dispatch-product-close/phase8 (just shipped).
5. TESTS: extend tests: (a) invoking run-core-offline via a 3-deep symlink from a fixture repo resolves REPO_ROOT to THAT fixture repo (not parent); (b) canonical invocation resolves to the invoking checkout/worktree root (existing behavior preserved — suite counts unchanged); (c) non-git location → repo_root_unresolvable exit nonzero, zero suites run. Register in run-core-offline.sh itself.

ACCEPTANCE: bash -n (bash5 + /bin/bash 3.2); focused tests green; FULL run-core-offline.sh green vs current-main baseline (verify count on THIS main first — 32 as of 9158921); a manual probe `bash ~/Projects/persona-engine/.claude/scripts/tests/run-core-offline.sh` (or its header logic) resolving REPO_ROOT=persona-engine printed in the report. Do NOT commit.
Rollback: git checkout of touched files.
