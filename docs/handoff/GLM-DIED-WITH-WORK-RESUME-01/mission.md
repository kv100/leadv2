# GLM-DIED-WITH-WORK-RESUME-01 — product-close must resume a died-with-work run once before gating its partial diff

Repo: ~/Projects/leadv2 (canonical plugin). Founder complaint 2026-08-03: "GLM падает или недописывает". Evidence-based split showed 5/6 of the day's GLM runs completed fine; the ONE real death (run 260803-173809-99f0fe0f, task M-8) was a stall-guard SIGTERM (exit 76 wrapper / RUN_FAILED 143) mid-write after real work (~36k output tokens). The run's own classifier wrote `outcome: died-with-work` into meta.yaml — and NOTHING acted on it: leadv2-dispatch-product-close.sh gated the PARTIAL diff, e2e failed, and the lane got a permanent write-once `dead/e2e_regression`. A recoverable death became a permanent burial.

MECHANISM (verified):
- `glm-coder.sh:1504` calls `leadv2-lane-outcome.sh <run_dir> <exit_code>` at finalize; it writes exactly one of `completed | died-with-work | died-clean` into meta.yaml (`outcome:`). `died-with-work` = the run died on a bound (stall/turn-cap/wall-clock) WITH a real work delta present.
- glm-coder.sh already has internal stall-REVIVE machinery (`revived_from` recursion guard :1315-1351, quota-gate check on revive, REVIVE_BLOCKED_BY_GATE) — but it only fires for its own in-flight stall detection, not after finalize.
- leadv2-dispatch-product-close.sh reads meta.yaml for liveness (pc_worker_alive knows revived/revive_blocked_by_gate since 1674dde) but NEVER reads `outcome:` — after worker exit it goes straight to diff measurement + gates.

REQUIREMENT:
1. RESUME-ONCE ON DIED-WITH-WORK: in leadv2-dispatch-product-close.sh, after the worker exits and BEFORE the diff is gated: if the lane's run meta says `outcome: died-with-work` AND no resume has been attempted for this lane yet (persistent marker in the lane's handoff dir, e.g. docs/handoff/<task>/.dwr-resume-attempted — worktree-local is fine), then resume/continue the SAME run once (retry policy §4.3 of docs/leadv2/single-lead-mode.md in persona-engine — same-run resume, not a fresh dispatch): reuse the existing revive path if callable from outside glm-coder (e.g. invoking glm-coder.sh with the run_dir / revived_from contract), else relaunch the worker on the SAME worktree with the same mission + a short prefix noting prior partial work exists. The resume must pass the same quota gate the internal revive uses (REVIVE_BLOCKED_BY_GATE handling: if the gate refuses, fall through to gating the partial diff as today — with the refusal journaled).
2. AFTER the resumed run settles (normal pc_await_worker_exit on the new run handle), gating proceeds exactly as today on the final diff. Second death (any outcome) = no further resumes; gate whatever exists.
3. JOURNAL: emit decision lines `dwr_resume task=<sig8> from_run=<run_id> new_run=<run_id|blocked_by_gate|launch_failed>` so the lead sees it happened.
4. KIMI PARITY: kimi-coder.sh uses the same lane-outcome classifier — the product-close logic must key off meta.yaml `outcome:` generically (glm/kimi both), not off the provider name.
5. NON-GOALS: no changes to lane-outcome classification, no changes to write-once ledger semantics, no dispatch-code.sh changes (LANDED-AT-SPAWN-01 is a separate task — if it is in flight in another lane, do NOT touch leadv2-dispatch-code.sh at all), no multi-resume loops (exactly once, marker-guarded), no supervisor changes.
6. TESTS: extend tests/test-no-work-terminal.sh or sibling with stubbed run dirs: (a) died-with-work + no marker → resume invoked once (stub launcher), marker written; (b) marker present → no second resume, straight to gating; (c) outcome=completed/died-clean → no resume; (d) resume blocked by quota gate → journaled + gating proceeds on partial diff; (e) kimi meta shape gets same treatment. Register any new suite in run-core-offline.sh.

ACCEPTANCE: bash -n (bash5 + /bin/bash 3.2); focused suites green; run-core-offline.sh green vs current-main baseline (verify suite count on THIS main first). Report per-requirement status. Do NOT commit.
Rollback: git checkout of touched files.
