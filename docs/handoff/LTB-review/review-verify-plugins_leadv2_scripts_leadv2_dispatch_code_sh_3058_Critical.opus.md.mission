Try to REFUTE this finding. Default is_real=false if you cannot concretely confirm it against docs/handoff/LTB-review/build.diff.
Finding [Critical/correctness]: diff does not apply to current main (git apply --check fails) — HEAD already carries a different, unrelated fix (STATUS-SURFACE-SHOWS-STALE-TRUTH-01 C5) that removed the founder_task_id gate and introduced `reg_id="${founder_task_id:-dispatch-${sig8}}"`; the diff's Row-2 claim ("registration only on fanout path") is stale and its context lines don't match, so this cannot be merged as-is without a manual rebase that risks silently dropping the log_path stamp or duplicating registration-key logic (reg_id vs _reg_key).
Report exactly one line: VERIFY_VERDICT: <upheld|refuted>
