Try to REFUTE this finding. Default is_real=false if you cannot concretely confirm it against docs/handoff/LTB-review/build-r2.diff.
Finding [High/correctness]: sha256sum failure is masked by the pipeline so the `|| return 1` guard never fires; two empty hashes compare equal and a CHANGED divergent copy is silently not quarantined while the log claims it was preserved
Report exactly one line: VERIFY_VERDICT: <upheld|refuted>
