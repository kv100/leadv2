Try to REFUTE this finding. Default is_real=false if you cannot concretely confirm it against docs/handoff/LTB-review/build-r2.diff.
Finding [High/correctness]: the Row-1 "mutation gate" is a grep-on-source (mission forbids these); mutating the stamped log_path back to pulse.md reintroduces the exact defect and the suite still reports pass=15 fail=0
Report exactly one line: VERIFY_VERDICT: <upheld|refuted>
