# ПОПРАВКА К МИССИИ — читать до Шага 1, она отменяет названный механизм

Лид, 2026-09-04, после более точного замера. Симптом подтверждается, **названная причина
была неверной**. Не трать время на опровержение уже опровергнутого.

## Что отменяется

Формулировка «классификатор отдаёт `class=product`, а гейт принуждает только
`Standard|Heavy|Strategic`, поэтому линия проскакивает» — НЕВЕРНА. Строка
`dispatch_classified class=product reason=conservative_default` — это ПРЕДВАРИТЕЛЬНАЯ проба,
а не разрешённый класс линии.

## Что измерено на самом деле (журнал `docs/leadv2/tasks/dispatch-96d97702/journal.md`, 42 строки)

- строка 5: `task_class=Standard route=phases source=flag`
- строка 8: `brain_decision class=Standard class_source=floor_held phases=classify,plan,gate1,build,test,review,deploy,live_verify,close`
- `lane_plan_missing reason=source_absent source=.../context.yaml` — плана НЕТ
- строк, содержащих `phase_precondition`: **НОЛЬ**

То есть класс разрешился как `Standard`, полный список фаз объявлен, `plan` физически
отсутствует — и гейт, который обязан на `Standard` уйти в `mode=1` и ОТКАЗАТЬ
(`leadv2-dispatch-code.sh:4166-4173`), не отказал и не оставил ни одной строки.

Для сравнения: линия `dispatch-4e975d5a` в тот же день записала
`phase_precondition_refused class=Standard missing=plan,gate1 mode=1`. Значит механизм
РАБОТАЕТ где-то и НЕ работает здесь — вот это и есть настоящий вопрос.

## Новый вопрос №1 (заменяет прежний вопрос 1 Шага 1)

**Почему `_phase_precondition_guard` не оставил следа на линии `96d97702`, хотя на `4e975d5a`
оставил?** Кандидаты, проверяй каждый ФАКТОМ (файл:строка + журнальная строка), не выбором:

- гейт не был вызван на этом пути (два места вызова: `:7372` и `:8594` — какой из них отработал);
- вызван, но с другим значением класса, чем разрешённое (`task_class` на момент `:7372`);
- вызван с `--waiver` (см. `_phase_waiver_args`, `:7368`, `:8588`) — кто и на каком основании;
- `PHASE_GUARD_SCOPE=pre-build` сузил проверку так, что отсутствие плана в неё не попало;
- `REQUIRE_PHASES` (по умолчанию `warn`, `:773`) был выставлен в окружении линии.

**Разница между двумя линиями — самый дешёвый путь к ответу.** Возьми обе, найди первое
расхождение. Не начинай с чтения всего файла.

## Что в миссии остаётся в силе

Всё остальное: пункт 1.3 (красная G1: запись отсутствует или сюита ищет не там — это два
разных бага), пункт 1.4 (сколько живых линий за сутки прошли без строки `phase_precondition`
— считай по всем журналам, это и есть масштаб), правка (B) «`warn` обязан оставлять след»,
и весь Шаг 3 с тремя негативными контролями.

Правка (A) переформулируется по итогу нового вопроса №1: чинить надо ту причину, которую ты
найдёшь, а не ту, которую я назвал. Если окажется, что дыра шире (гейт молчит на целом классе
путей) — так и напиши, это более важная находка, чем та, за которой я тебя послал.
