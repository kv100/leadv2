Try to REFUTE this finding. Default is_real=false if you cannot concretely confirm it against docs/handoff/PLAN-FOLLOWUPS-01/build-attempt-2.diff.
Finding [High/correctness]: Caller `elif rank_table:` ignores floor_ok, so --plan-pool still returns pool_floor_table_degenerate on a valid ladder — the docstring's stated contract is unhonoured
Report exactly one line: VERIFY_VERDICT: <upheld|refuted>
