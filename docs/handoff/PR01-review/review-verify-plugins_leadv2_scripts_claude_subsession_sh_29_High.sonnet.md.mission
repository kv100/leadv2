Try to REFUTE this finding. Default is_real=false if you cannot concretely confirm it against docs/handoff/PR01-review/build.diff.
Finding [High/correctness]: ROLE_SOURCE=agents_worktree_fallback fails the `== "agents"` test at claude-subsession.sh:190, so the fallback injects raw YAML frontmatter as the system prompt and loads zero skills
Report exactly one line: VERIFY_VERDICT: <upheld|refuted>
