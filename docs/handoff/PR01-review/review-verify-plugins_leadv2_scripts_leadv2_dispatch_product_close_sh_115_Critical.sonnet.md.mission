Try to REFUTE this finding. Default is_real=false if you cannot concretely confirm it against docs/handoff/PR01-review/build.diff.
Finding [Critical/correctness]: _pc_reap_worker SIGTERM/SIGKILLs the same self-matched pgrep list including its own PID, killing the close gate before it writes review-gate.md and the terminal ledger row
Report exactly one line: VERIFY_VERDICT: <upheld|refuted>
