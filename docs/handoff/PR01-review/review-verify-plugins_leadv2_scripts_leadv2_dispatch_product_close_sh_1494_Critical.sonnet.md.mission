Try to REFUTE this finding. Default is_real=false if you cannot concretely confirm it against docs/handoff/PR01-review/build-r2.diff.
Finding [Critical/correctness]: _pc_reap_worker called with ${HANDLE} where the signature requires <run_dir>, so the timeout reap reads no pgid/.lockref and never kills the lock-holding __supervise process
Report exactly one line: VERIFY_VERDICT: <upheld|refuted>
