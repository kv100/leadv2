Try to REFUTE this finding. Default is_real=false if you cannot concretely confirm it against docs/handoff/PR01-review/build-r2.diff.
Finding [High/correctness]: pgid files hold a setsid process-GROUP id but are killed as bare pids (kill -TERM "$pid" not -"$pid"), so only the group leader dies and the model child keeps burning quota
Report exactly one line: VERIFY_VERDICT: <upheld|refuted>
