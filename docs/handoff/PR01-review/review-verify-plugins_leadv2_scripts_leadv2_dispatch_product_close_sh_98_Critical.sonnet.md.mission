Try to REFUTE this finding. Default is_real=false if you cannot concretely confirm it against docs/handoff/PR01-review/build.diff.
Finding [Critical/correctness]: _pc_process_alive pgrep -f "$handle" self-matches the close gate itself (HANDLE is its own argv[4]), so it always reports alive and every glm/kimi lane now burns the full 4200s ceiling
Report exactly one line: VERIFY_VERDICT: <upheld|refuted>
