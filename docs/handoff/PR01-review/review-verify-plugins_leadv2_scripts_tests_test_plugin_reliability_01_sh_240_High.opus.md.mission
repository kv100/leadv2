Try to REFUTE this finding. Default is_real=false if you cannot concretely confirm it against docs/handoff/PR01-review/build.diff.
Finding [High/test]: None of the D1/D2/D4 tests source or invoke the real functions (_pc_process_alive, _pc_reap_worker, pc_worker_alive, or the claude-subsession.sh worktree-fallback block). D1/D4 "tests" simulate the liveness decision with hand-rewritten shell (sed one-liners) instead of the shipped code, and D2 reimplements the common-dir derivation independently in the test file. A regression in the actual sourced logic would not fail this suite despite it being wired into run-core-offline.sh as the regression gate for this PR
Report exactly one line: VERIFY_VERDICT: <upheld|refuted>
