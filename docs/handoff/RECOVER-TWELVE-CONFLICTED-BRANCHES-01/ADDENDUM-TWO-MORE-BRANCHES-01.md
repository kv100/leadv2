# ADDENDUM — две ветки сверх списка

Лид, 2026-09-04, уже после старта твоей линии. Пока ты работала, ещё две линии умерли по
простою (80 минут без процесса, вердикт положительный: процесса нет, простой измерен). Их
работа закоммичена и цела, но в main не сливается.

Добавь их в свой список **после** двенадцати исходных, в том же порядке приоритета — сначала
однохунковые:

| Ветка | Конфликт | Что в ней |
|---|---|---|
| `worktree-BROAD-STATUS-READY-FIRES-ON-A-DAY-OLD-FILE-01` | `plugins/leadv2/hooks/leadv2-single-lead-beat.sh` | 5 коммитов — почему статус-пульс срабатывает на суточной давности файле |
| `worktree-SMART-ARBITER-01` | уже слита тобой коммитом `b718c1ec` | ничего не делай, отмечено для полноты |

Несохранённое в обеих проверено мной: только контрольная плоскость и кэш сводок, настоящей
работы вне коммитов нет. Поэтому сливать надо ровно ветки, ничего доставать из рабочих
деревьев не нужно.

Правило разрешения то же: не «взять наши/взять их», а сохранить оба намерения. Хук
`leadv2-single-lead-beat.sh` — живой путь статус-пульса, и он сегодня уже соврал: пульс
печатал `live=0 нет-журнала` по всем линиям в момент, когда одна из них точно правила файлы.
Если увидишь, что одна из сторон конфликта чинит именно это — она важнее, сохрани её целиком.
