# RECOVER-WAVE1-TEN-01 — десять веток волны В1

Слияние десяти конфликтных веток В1 в main (контрольная плоскость — ours через петлю
recover-merge.sh; конфликты кода — по смыслу, union обоих интентов). Порядок: снизу вверх
по объёму. Доказательство на каждую: sha, непустой `git show --stat`, 8 симлинков
docs/leadv2/ целы после КАЖДОГО слияния.

| # | Ветка | sha | Судьба | Как разрешён конфликт |
|---|---|---|---|---|
| 1 | `worktree-d784b987` (CLAIM-EVIDENCE-GATE-01, 592+) | `4ee5bc8d` | СЛИТО | dispatch-code/helpers/run-core-offline: все 4 хунка «HEAD добавил, ветка не трогала» — сторона HEAD, чисто смерженные вставки ветки (инъекция evidence-contract, регистрация сюиты) сохранены; add/add `test-claim-evidence-gate.sh` за HEAD — строгое надмножество (её же сюита + фиксы PPC-G1/BURN-GOVERNOR). Сюита 25/0. |
| 2 | `worktree-BROAD-STATUS-READY-FIRES-ON-A-DAY-OLD-FILE-01` (620+) | `678f7dea` | СЛИТО | single-lead-beat.sh union: резолвер-маршрутизация founder-status из HEAD (§0b) + case-анкеровка и вся epoch-машина ветки; epoch-путь raw зеркально write-стороне (пара status/epoch в одном checkout). Стабам 3 сюит добавлен leadv2-portable-lock.sh (main-резолвер источает его :86; без копии хук молча терял CTX — T4-T6/T19 красные на всех трёх версиях хука, A/B доказано). stale-file 7/0, relay-scope 25/0 (была красной на pre-merge), beat-stamp 6/0; duty rc=124 ПРЕДСУЩЕСТВУЮЩИЙ красный (A/B premerge-дерева: тот же rc=124, те же T3b/T4a-c). |
| 3 | `worktree-PLUGIN-RELIABILITY-01` (687+) | `3424ace2` | СЛИТО | product-close хунк1 за ветку (D4 grace-guard — единственное уникальное), хунки2-3 за HEAD (call-sites `_pc_run_dir_for` + stop-gate соответствуют смерженной сигнатуре `<run_dir>`; хэндл-стиль ветки ломал бы вызов); dispatch-code/run-core-offline/сюита за HEAD-эволюцию (reliability-02 уже в main, регистрации :458-459 обе есть). Сюита 21/0. Игнор-журналы: rm --cached (байты не тронуты). |
| 4 | `worktree-049e0e9e` (REVIEW-GATE-INFRA-01, 712+) | `27f3bd83` | СЛИТО | Все 10 хунков за HEAD: ветка суперседирована — её round-2 уже в main (8cc6bf8c + починка 85ae8860; битые байты стороны ветки = до-починное состояние). Ценность мерджа: +81 строка product-close авто-мерджем (`_pc_norm_write` mixed write-set нормализация). scope-evidence 7/0; verdict-recovery 240s-hang с байтами, идентичными pre-merge main (`git diff 3424ace2` пуст) — предсуществующий, не мердж. |
| 5 | `worktree-100a892d` (PLUGIN-REVIEW-ARMS-01, 761+) | `2475b362` | СЛИТО | routing.yaml: старый ручной минимум 2026-08-15 (phases/downgrade_chain) заменён tenant-копией СЕГОДНЯШНЕГО canonical + delta ветки — protected_path_patterns = UNION shipped-дефолта (safety/publish/payments) и repo-глобов ветки (scripts/hooks/config/settings.json); эволюция main (effort_matrix, failure_memory, glm-5.3) сохранена, legacy-ключи HEAD никем не читаются (router.sh think-model → fable rc=0 на новой форме; regex-экстрактор glm_policy 52 строки; safe_load ок: ladder 9, patterns 10). review-run.sh 2 хунка union: HEAD-база (FP-07B fail-closed на отсутствие pool=, персистентный review-pool-resolver.err, emit decision) + stdout-строки ветки resolver_rc=/resolver_stderr= (их читает §3.1 unreviewed-артефакт и hunk2-парсинг — обе стороны чинили ОДИН дефект, ветка сама ссылается на HEAD-фикс dispatch-8e2a32be A2); hunk2 = обе вставки (парсинг ветки + security/reroute HEAD). dispatch-code.sh 1 хунк: сторона ветки пуста (HEAD добавил FOREIGN-ROOT-GUARD/REPORT-ONLY-GATE/BURN-GOVERNOR, ветка не трогала) — за HEAD. Сюита 28/0 rc=0 (timeout 240, код сюиты), зарегистрирована в run-core-offline (:322, локальный раннер вне git). bash -n 2/2. ВАЖНО: коммит создан ПАРАЛЛЕЛЬНОЙ сессией в 07:07 как «WRITESET-CAROUSEL-01 report» поверх моего резолвнутого индекса (Merge: 27f3bd83+f76a205e=ветка; git diff 2475b362 по 3 файлам пуст = мои байты); сообщение чужое, содержание верное, историю main не переписывал. Симлинки 8/8 после слияния. |
| 6 | `worktree-f7f1c2c8` (REPORT-ONLY-GATE-01 round-2, 1299+) | `2a0e5848` | СЛИТО | Ветка = round-2 того же лейна (codex-фиксы 2026-08-17, в main не лежали; main имеет только round-1 eb8d8a6f). lib/leadv2-report-deliverable.sh: оба хунка за ветку — symlink/parent-dir containment + hardlink st_nlink guard + harvest fail-closed (чистые добавления, HEAD-сторона = старое тело). product-close 12 хунков: х5/х6/х10 за ветку (laundering-guard unscoped_lane_work для report-лейнов, mission-excerpt в review-входе, review читает harvested dest, prose rubric clause e), остальные 9 за HEAD-эволюцию (DoD-gate, LANE-OBSERVABILITY-02, T11-D1, write-terminal arity); skip-emit ветки (review_engine skipped reason=report_lane) уже есть в HEAD вне конфликта. dispatch-code 11 хунков: 8 за HEAD; х9 union (компакт-форма HEAD + elif non-product unenforced ветки r2-f1), х10 union (deliverable-recovery при advance-arm ветки + T13-машина ре-арбитража HEAD), х11 union (arg4 candidate_arms CSV HEAD + arg8 _adv_deliverable ветки; сигнатура spawn_product_close 8-арг подтверждена). Сюита: механика HEAD (PRE_GATE_REF red-first, valid-Python seed, foreign-root guard) + кейсы C7-C11 ветки. developer-артефакты за round-2 ветки, REPORT-ONLY-GATE-01/report.md за HEAD (полнее). Сюита: 10 passed / 3 failed (C4/C6b/C6c) — A/B на pre-merge 2475b362 в изолированном worktree: ТЕ ЖЕ 3 красных (5/3 там) — предсуществующие, мердж добавил 5/5 зелёных C7-C11 и не регрессировал; rc=1 обеих. bash -n 5/5. Симлинки 8/8 после слияния. |

## Отчёт по сюитам (передний план, код возврата сюиты)

- `test-claim-evidence-gate.sh` rc=0 — 25 passed(red->green), 0 failed.
- `test-broad-status-stale-file.sh` rc=0 — 7 passed, 0 failed (после стаб-фикса portable-lock; до фикса 4/3 — красное доказано на всех трёх версиях хука).
- `test-broad-status-relay-scope.sh` rc=0 — 25 passed, 0 failed (до стаб-фикса T19 красный; на pre-merge main красная T14/T18/T19).
- `test-beat-stamp-agreement.sh` rc=0 — 6 passed, 0 failed.
- `test-broad-status-duty.sh` rc=124 (240s) — предсуществующий красный pre-merge main, A/B: premerge-дерево rc=124, те же T3b/T4a/T4c.
- `test-plugin-reliability-01.sh` rc=0 — passed=21 failed=0.
- `test-review-gate-scope-evidence.sh` rc=0 — 7 passed(red->green), 0 failed.
- `test-review-verdict-recovery.sh` rc=124 (240s, и после коммита) — байты идентичны pre-merge main, предсуществующий hang, не вызван мерджем.

## Симлинки

После каждого из 4 слияний: `.bus-offsets .bus.lock .merge.lock active.yaml.lock bus.jsonl
merge-queue.jsonl open-threads.md questions` — все 8 `-L`, ни одного BROKEN.
