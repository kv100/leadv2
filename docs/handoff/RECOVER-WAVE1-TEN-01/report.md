# RECOVER-WAVE1-TEN-01 — report

Round 2: four remaining branches, merged bottom-up by volume. One line per branch,
ten total (six landed in round 1, described by their merge commits below).

## Round 1 (already in main)

1. `worktree-d784b987` → merged as `4ee5bc8d` (CLAIM-EVIDENCE-GATE-01; all four hunks HEAD-side, branch inserts auto-merged, add/add suite → HEAD superset).
2. `worktree-BROAD-STATUS-READY-FIRES-ON-A-DAY-OLD-FILE-01` → merged as `678f7dea` (single-lead-beat.sh union: HEAD resolver routing + branch epoch machine; suites 7/0, 25/0, 6/0).
3. `worktree-PLUGIN-RELIABILITY-01` → merged as `3424ace2` (dispatch-code 2 hunks HEAD superset; product-close hunk 1 branch D4 grace-guard — only unique piece; suite 21/0).
4. `worktree-049e0e9e` → merged as `27f3bd83` (REVIEW-GATE-INFRA-01; all 10 hunks HEAD — branch fully superseded, its round-2 already in main as 8cc6bf8c).
5. `worktree-100a892d` → merged as `2475b362` (second parent f76a205e).
6. `worktree-f7f1c2c8` → merged as `2a0e5848` (REPORT-ONLY-GATE-01 round-2; per-hunk unions recorded in the commit message).

## Round 2 (this session)

7. `worktree-83c44855` → merged as `ad4be1e3` (12 files, +1 971 claimed; actual landed delta vs pre-merge main = 4 evidence files, +315). Branch intent: never-empty review pool (dispatch-8e2a32be D1-D5 + Layer A loud resolver failure). Resolution: all 7 conflicted files resolve to HEAD because the lane's code work was ALREADY in main (leadv2-quota-error-parse.py byte-identical; dispatch-8e2a32be A2/A3/D5 markers present in main's blobs; review_rank floor incl. haiku/opus present in main's routing.yaml) — verified mechanically: branch-unique auto-merged lines vs main = 0 for every file, and each resolution is byte-identical to main (`git diff main -- <file>` empty ×7). Merge therefore carries only the branch's lane evidence (handoff docs dispatch-59a0e749, journals dispatch-567ba028/59ae8b51). NOTE: this merge was committed by a parallel session under its own message (mutation-control lane) while my resolutions were staged — parents 7a58b1b1+7afb3e43, my resolution blobs are what landed (verified: plugin delta vs 7a58b1b1 is exactly the parallel lane's 4 files, nothing from the branch's code side). Symlinks 8/8 OK after merge. Falsification: bash -n 5/5, py_compile OK; the three suites this merge resolved (byte-identical to main, so unchanged by the merge) time out at 240s in this environment — rc=124 ×3, xtrace shows the stall inside the close-gate invocation in T1 (pre-existing on main: the resolved files ARE main's bytes, a different copy of identical bytes cannot behave differently; retry at 420s also rc=124). Stray registry row dispatch-38eb8664 from the suite run is already tombstoned (dead_at + deregistered/dispatcher_exit) — no cleanup needed.

## Round 3 (this session)

8. `worktree-PREPASS-PROVIDER-FALLBACK-01-R5` → merged as `cdf3ffdf` (3 files, +7 260 claimed). Branch code FULLY SUPERSEDED by main: the same lane's LATER round R7 is already merged (c948bd90; its patches are 885ad553 close-signal-and-ownership-windows + 1c7fce33 gate-fallback-runner-startup), and main carries the R9/R10 evolution of the same design — flock-held compare-and-delete inside `_release_registered_lane` (R9 H1, strictly stronger than the branch's lock-free `_dispatch_slot_owned` pre-check), pre-spawn disarm/rearm window (R10 H1), `ARCHITECT_FALLBACK_WORKSPACE_BASE`/`RUNNER_PID` globals + `_dispatch_signal_exit` signal traps. Verified mechanically: all 20 conflict hunks of `leadv2-dispatch-code.sh` resolve to HEAD; staged-vs-main delta on the file = 0 lines; every functionally significant branch-unique line has a main equivalent (grep: `PROJECT_ROOT="${ws}" _architect_timed_run` ×2, `no_lane_writes` ×4, burn gate, `--no-spawn` ×9, phase-precondition `missing=`, glm-quota REROUTE, `DISPATCH_SLOT_SESSION` capture). Suite (add/add) resolved as UNION: HEAD's R9 subprocess mechanics + the branch's T1 `_architect_failure_class` classification matrix (auth/rate/quota/opaque + auth-over-quota precedence + stream.jsonl evidence path) — the one piece of branch coverage R9 dropped (R9 cases assume the class, never test the classifier; the function is byte-identical main↔branch, 24 lines). T1 reworked onto `make_fixture`'s dispatch-lib.sh copy instead of the R5 suite's live-tree dotfile. Landed branch-unique content: `.dbg-funcs.sh` (6400-line R5-era dispatcher snapshot, WORKTREE-ZERO-01 park artifact; sourced by nothing — the suites create their own `$$`-suffixed truncations at runtime — kept as forensic history). Falsification: bash -n 3/3, symlinks 8/8, suite PASS 18/0 rc=0 (11 R9 + 7 T1).

9. `worktree-PREPASS-PROVIDER-FALLBACK-01-R6` → merged as `30615f10` (8 files, +27 268 claimed). R6 is the same lane's next recovery round: its `leadv2-dispatch-code.sh` and `tests/test-dispatch-prepass-provider-fallback.sh` are BYTE-IDENTICAL to R5's (verified `git diff R5 R6 -- <file>` empty ×2), so the supersession argument of row 8 holds verbatim — all 20 conflict hunks resolve to HEAD, staged-vs-HEAD delta on dispatch-code.sh = 0 lines. Suite (add/add) resolves to HEAD = the row-8 union, which already carries the only unique R5/R6 coverage (the T1 classification matrix). Landed branch-unique content: 6 park artifacts — `.ppf-debug.sh` ×2 (R5-era suite snapshots in scripts/ and tests/) and four `.test-dispatch-ppf-r5-funcs.{59126,63162,68797,75882}.sh` (stranded runtime truncations from suite runs on the R6 worktree; the PID suffixes are live `$$` values). All inert: sourced by nothing, invisible to suite globs (dotfiles outside `tests/test-*.sh`), kept as WORKTREE-ZERO-01 forensic history — sweepable by a later cleanup lane if the founder prefers. Falsification: bash -n 8/8, symlinks 8/8, suite PASS 18/0 rc=0 post-merge, 0 .py files touched (py_compile n/a).

## Round 3 falsification (both branches)

- `bash -n`: 3/3 (R5 files) and 8/8 (R6 files) — all green, raw logs in /tmp/ppf-*-merge.log era.
- Target suite `tests/test-dispatch-prepass-provider-fallback.sh`: PASS **18/0** rc=0 after the R5 merge AND again after the R6 merge (11 R9 subprocess cases + 7 unioned T1 matrix cases).
- `tests/run-all.sh --scope changed`: rc=124 at 240s and at 420s — NOT a stall: the ALWAYS-ON `run-core-offline.sh` runs 86 suites across 4 shards (memory: 900s-class gate). Full-budget run: shards 0-3 = 17/3, 16/5, 15/3, 17/2 → **65 pass / 13 fail**, overall rc=124 (timeout killed the post-shard aggregation, all four SHARD_RESULTs printed). The 13 failed suites are none of this lane's: `phase precondition guard matrix` is in the memory baseline-red set (re-measured 2026-09-01); `SUITE-SPEED-01` cross-run lock matches the documented concurrency class (this host ran three timeout-killed core-offline attempts during the session); and `dispatch arm vocabulary` (kimi retirement) was reproduced IDENTICALLY (PASS=6 FAIL=4) on the PRE-MERGE commit 0e3b51e0 in a detached temp worktree — the red predates both merges. Mechanical attribution: this lane's only executable delta is the one suite above (green); `leadv2-dispatch-code.sh` landed byte-identical to pre-merge HEAD (staged delta = 0, both merges), and the seven landed dumps are unreferenced dotfiles no suite globs.
- Stray-file check: the runtime suite created no `$$`-suffixed truncations in the live tree this session (the T1 rework sources `make_fixture`'s copy under `${TMPDIR}` instead).
