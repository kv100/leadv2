verdict: APPROVE
next_action: continue

DECISION_OPTION: b — authorize the minimal outcome-writer change.

- Grep of `leadv2-lane-outcome.sh`: zero hits for arm/model/work_kind/complexity; all sinks key on RUN_DIR only.
- So option (a)'s ledger begins empty and stays empty — capability ships inert.
- Authorization is additive-only, fail-open, this file only.

Full: architect.full.md
