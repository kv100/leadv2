verdict: APPROVE
next_action: continue

Design written to docs/audits/one-plugin-source-fable.md: one byte store, per-file symlinks everywhere, declared per-project layer.

- Projects are already 99.9% symlinks; the 544/351/101 real copies are the plugin's own rsync targets (f)(b)(e).
- 24 of 64 overrides unread; core yaml overrides carry live decisions.
- Pre-commit guard wired in zero repos; m3-market exists at ~/MythicalGames (not git).

Full: full.md
