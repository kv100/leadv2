verdict: APPROVE
next_action: continue

Diagnosed: the spill walk deletes `base_arm` (glm) from the candidate chain, so a codex-fitting
mission whose Codex probe reads unknown (live HTTP 401) lands on Sonnet — 90 journal rows. Nothing
is cached; the exclusion is structural.

- Skip `base_arm` only for exclusion rules, not the codex-preference rule
- A known over-threshold GLM still steps aside; additive `readings=` line for legibility
- No timer, no cache, no reason/rule string changes

Full: architect.full.md
