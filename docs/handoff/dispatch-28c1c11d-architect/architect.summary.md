verdict: APPROVE
next_action: continue

Design scoped: land the worktree's review-base fix behind a red-first test, add `--hours` stand-down to `record-quota-lockout`, decide the dispatch door by reproduction.

- Fault B is **not** Fault A: builder already passes `--cwd`; real gap is enqueue-treated-as-spawn (no first-byte proof).
- Stand-down is additive — lockout reader unchanged.
- Stage only LANE_WRITES; worktree carries unrelated journal churn.

Full: architect.full.md
