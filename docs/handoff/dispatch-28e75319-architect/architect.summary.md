verdict: APPROVE
next_action: continue

Design ready: 12-site effort audit found 4 real gaps (no-tier path unpinned, fallback effort=high, planner tier drift, planner top ungated).

- New shared gate lib + circuit marker in control-plane dir; circuit fails CLOSED, live-quota stays fail-open.
- New PreToolUse Bash hook; dispatch-code needs zero edits.
- 20-case suite + run-core-offline registration.

Full: architect.full.md
