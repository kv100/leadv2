verdict: APPROVE
next_action: continue

Two separate codex faults, scoped repro-first.

- Review arm: `--base HEAD` on committed work = empty diff → empty body → `review_body_lost`; codex also passes no `--cwd`.
- Dispatch arm: `--write` proven inert; ranked probes A1 tier→model, A2 worktree registry, A3 argv mission, A4 stdin.
- New `stand-down` verb replaces quota-only lockout.

Full: architect.full.md
