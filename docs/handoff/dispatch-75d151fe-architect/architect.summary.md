verdict: APPROVE
next_action: continue

Path (B) for product-close (flag-0 lane keeps its inline owner), (A) for the JS. Census becomes a reachability census: two flag-keyed buckets plus a fail-on-unclassified clause.

- `leadv2-review.js` deleted — 3 byte-identical copies confirmed; the 3 suites' coverage migrated, not dropped.
- Doc-pointer port into the engine's codex prompt is a live lead-path change — flagged.
- Negative control prescribed to prove the census still goes RED.

Full: architect.full.md
