verdict: APPROVE
next_action: continue

Not a stale cache — `leadv2-status-surface.sh` holds TWO lane models; the widget reads the unfixed one (`render_single_lead`'s own census), while c1e8cbf fixed `_ss_lanes_py`.

- `· · ·` + `sig` row: widget whitespace-splits a `·`-delimited line (.5s.sh:463).
- Fix: `_ss_lanes_py` becomes sole model; `render_single_lead` = awk projection.
- New `cls=queued`, collapsed warnings, parity test.

Full: architect.full.md
