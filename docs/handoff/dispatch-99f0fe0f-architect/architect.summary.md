verdict: APPROVE
next_action: continue

# architect — M-8 skill Definition-of-Done

Design: per-skill sibling `PROOF.sh` (glob-is-the-registry, no drift, no frontmatter risk), graded by a standalone gate with a 5-value RED/GREEN vocabulary and hash-checked state.

- T1–T8 tautology check refuses no-fail proofs at discovery, before execution.
- memory-gc proof stubs `CLAUDE_BIN`, asserts exactly one batched call carrying `--model haiku`.
- Gate stays out of run-core-offline; only its unit suite is wired in.

Full: architect.full.md
