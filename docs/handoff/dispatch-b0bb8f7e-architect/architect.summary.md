verdict: NEEDS-INFO
next_action: continue

Design delivered: 3 Plan paths consolidate into bash `leadv2-plan-run.sh`; Diagnose folds in as a mode, no second engine.

- Both prerequisite docs (`design.md`, `mission-build-r1.md`) do not exist on disk — census rebuilt from source; §7 conclusions need them.
- Prepass writes `context.yaml`, so the real validator replaces the grep heuristic.
- Only the `dispatch-code.sh` swap serializes behind the review lane.

Full: docs/handoff/one-review-path-2026-08-06/design-plan-diagnose.md
