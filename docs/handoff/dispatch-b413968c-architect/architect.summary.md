verdict: REVISE
next_action: continue

Mission framing is wrong: codex/claude *review* ARE gated at 95 by the pool resolver. Real holes: codex *build* (yaml-conditional, session-runner path bypasses it) and claude *build* (no reader). Reroute already works — only the `codex_dead_reroute` journal line is missing, at two independent resolve points.

- 14 files; ceilings centralized into one sourced config, values unchanged.
- claude-build refusal left unwired (would block the lead's own reviewers) — founder call.

Full: architect.full.md
