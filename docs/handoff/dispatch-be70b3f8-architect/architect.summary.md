verdict: APPROVE
next_action: continue

Design closed: probe toplevel identity before the scope gate grades `_lane_root`; emit `lane_root_not_a_worktree`, zero offending paths.

- Second caller found — `pc_silent_arm_probe` (L1273) fails the same way and advances the arm on a clean parent. Guarded too.
- Mission item 4 premise wrong: no vocabulary enum exists; only the ledger doc comment needs the word.
- Counterexample survives: `_lane_root == main checkout` reproduces the symptom; evidence key only, follow-up named.

Full: architect.full.md
