# P1b — launch the chosen arm

Implementation checkpoint. The Python registry supplies Claude launch argv and launchability; incapable kinds refuse. GLM/freepool retain existing adapters. Opus uses the common reserve/spawn/confirm path. Unmetered accounts receive the least generous configured headroom weight on matrix cost and no unknown-probe penalty; unknown accounts retain 50.

Focused argv and account-state suites pass; pool suite passed 19/0 before strengthening the explicit-opus case to safety. Final self-check and mutation outputs will be appended below.

UNVERIFIED: live 401 acceptance. The current live team-account usage probe returned HTTP 200 (live-quota.log). A live launcher probe and the bounded changed-scope runner are in progress at this checkpoint.
