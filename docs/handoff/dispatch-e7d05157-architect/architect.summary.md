verdict: APPROVE
next_action: continue

# architect.summary — WHEN-TO-FORK-01

New doc `plugins/leadv2/docs/work-placement.md` states three ordered yes/no tests; three existing docs get pointers only.

- Test 1 diff/durable deliverable → lane; Test 2 only-this-session-knows → fork; else fresh agent.
- Verification = always fresh agent; report-only = lane only if cited after this session.
- Enforcement named (advisory PreToolUse), not built. No scripts touched.

Full: architect.full.md
