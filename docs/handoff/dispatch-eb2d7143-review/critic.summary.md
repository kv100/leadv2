FAIL — 3 Critical, 3 High. Two of three levers no-op in the incident shape.

- C1: counter+park die after the first refusal (glm benched out of candidate_arms) — probe shows count=1 for 2 sonnet fallbacks.
- C2: `--retry-all` can never retry (mission_path always empty; terminal row → already_terminal forever).
- C3: zero test coverage for `--retry-all`.

Full: critic.full.md
