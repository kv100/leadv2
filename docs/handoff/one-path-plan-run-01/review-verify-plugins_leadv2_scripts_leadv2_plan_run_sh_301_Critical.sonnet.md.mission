Try to REFUTE this finding. Default is_real=false if you cannot concretely confirm it against docs/handoff/ONE-PATH-PLAN-RUN-01/build.diff.
Finding [Critical/correctness]: diagnose mode dispatches the architect PLAN_YAML prompt but validates for root_cause+confidence, so every diagnose run exits 9 blocked/empty_response and root-cause.md is never written
Report exactly one line: VERIFY_VERDICT: <upheld|refuted>
