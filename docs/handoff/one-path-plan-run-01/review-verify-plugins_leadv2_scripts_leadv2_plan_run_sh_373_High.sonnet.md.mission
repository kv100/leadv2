Try to REFUTE this finding. Default is_real=false if you cannot concretely confirm it against docs/handoff/ONE-PATH-PLAN-RUN-01/build-r2.diff.
Finding [High/correctness]: extract_plan_yaml assumes PLAN_YAML: precedes the ``` fence while the mission prompt (line 307) instructs the opposite order, so real arm output leaks post-fence prose into the YAML and the merge fails
Report exactly one line: VERIFY_VERDICT: <upheld|refuted>
