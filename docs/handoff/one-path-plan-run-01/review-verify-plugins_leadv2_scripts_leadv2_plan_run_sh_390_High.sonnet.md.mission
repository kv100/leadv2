Try to REFUTE this finding. Default is_real=false if you cannot concretely confirm it against docs/handoff/ONE-PATH-PLAN-RUN-01/build.diff.
Finding [High/correctness]: planner is unbound in diagnose mode (assigned only at line 519, after the diagnose block) so a floor/degraded pool aborts under set -u with rc=1 and a mislabelled provider_error gate
Report exactly one line: VERIFY_VERDICT: <upheld|refuted>
