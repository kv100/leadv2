Try to REFUTE this finding. Default is_real=false if you cannot concretely confirm it against docs/handoff/ONE-PATH-PLAN-RUN-01/build.diff.
Finding [High/correctness]: regex `claude  +-p` (ERE) requires 2+ spaces before -p, so it never matches the realistic single-space `claude -p` invocation the engine's own header calls a "CRITICAL review finding" — the guard is blind to the exact regression it claims to catch (verified: injecting `claude -p "x"` into a copy of the engine still passes this check).
Report exactly one line: VERIFY_VERDICT: <upheld|refuted>
