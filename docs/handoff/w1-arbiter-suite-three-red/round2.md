# ПОПРАВКА ЛИДА 2026-09-10 — раунд 2, приёмка провалена

Читать ВМЕСТЕ с `brief.md` в этой же папке. Где расходятся — этот файл главнее.

## Моя посылка была неверна, снимаю её

В `brief.md` я утверждал, что `(g7)` и `(g7-gap)` доказывают **дефект кода**: будто арбитр отвечает
одной причиной на оба случая. **Это неправда, проверено по механизму, а не по тексту упавшего
теста.** В `lib/leadv2-route-arbiter.sh` две отдельные ветки: `:1294` печатает
`reason=no_capable_cell`, `:1310` печатает `reason=pool_empty_all_excluded`. Словарь причин никогда
не схлопывался. Значит устарели **все три** теста, а не один, и раунд 1 поступил правильно,
не тронув арбитр.

## Почему раунд 1 всё же не принят

Мутация лида на настоящем файле: я схлопнул `no_capable_cell` в `pool_empty_all_excluded` ВНУТРИ
тела арбитра (строки 1294-1295; `grep -c "no_capable_cell"` упал с 4 до 2, то есть обе строки кода
действительно изменены). Перепривязанная сюита после этого выдала **`SUMMARY: pass=27 fail=0`** —
она НЕ заметила исчезновения того самого различения, ради которого `(g7-gap)` существует.

Перепривязать утверждение к тому, что код печатает сейчас, — значит согласиться с любым поведением.
Это ложный зелёный, и по нему линию принять нельзя. Ровно эта болезнь описана в
`docs/leadv2/learnings.md` и стоила нам недель.

## Что требуется в раунде 2

1. `(d2)` — перепривязку к `capability_fit` **оставить как есть**: она верна и соответствует
   директиве основателя (cost — один взвешенный член среди N, а не единственное ранжирование).
2. `(g7)` и `(g7-gap)` — переписать так, чтобы они **краснели под названной ниже мутацией**.
   Мутация и есть приёмка, а не пожелание:

   в теле арбитра заменить `_record('refuse','none','none','no_capable_cell')` и соседнюю строку
   печати на вариант с `pool_empty_all_excluded`; после этого сюита ОБЯЗАНА покраснеть на
   конкретном названном тесте.

3. Отдельно проверить и сказать в отчёте: различает ли сюита **три** состояния отказа, а не два —
   политический срез (армы вырезаны политикой), дыра в словаре матрицы (нет подходящей клетки),
   и пустой пул после исключений. Если состояний в коде действительно три, тест обязан отличать
   все три; если два — сказать это прямо и назвать, где третье имя используется.

## Приёмка раунда 2

- `bash plugins/leadv2/scripts/tests/test-route-arbiter.sh` → `pass=27 fail=0`, число назвать явно.
- Прогон под названной мутацией → сюита **красная**, с указанием конкретного упавшего теста.
- Оба прогона целиком в отчёт, плюс артефакт в
  `docs/handoff/w1-arbiter-suite-three-red/mutation-control/`.
- Лид повторит мутацию независимо на настоящем файле. Сюита, согласная с любым поведением, снова
  принята не будет.
